<x-layouts.app title="Arabuluculuk"><div class="flex flex-wrap items-end justify-between gap-4"><div><p class="text-xs font-semibold uppercase tracking-[0.18em] text-teal-600">Uyuşmazlık Çözümü</p><h1 class="mt-1 text-3xl font-bold">Arabuluculuk</h1></div><a class="rounded-lg bg-teal-700 px-4 py-2.5 text-sm font-semibold text-white" href="{{ route('mediations.create') }}">Yeni Kayıt</a></div><div class="mt-6 grid gap-4 md:grid-cols-2">@forelse($mediations as $mediation)<a class="rounded-xl border border-slate-200 bg-white p-5 shadow-sm" href="{{ route('mediations.edit',$mediation) }}"><div class="flex justify-between gap-3"><span class="font-mono text-xs font-semibold text-indigo-600">{{ $mediation->caseFile->case_no }}</span><span class="rounded-full bg-teal-50 px-2 py-1 text-xs text-teal-700">{{ $mediation->status->label() }}</span></div><h2 class="mt-3 font-semibold">{{ $mediation->mediation_file_no ?: 'Dosya numarası belirtilmedi' }}</h2><p class="mt-1 text-sm text-slate-500">{{ $mediation->mediator_name ?: 'Arabulucu belirtilmedi' }} · {{ $mediation->meeting_date?->format('d.m.Y H:i') ?: 'Toplantı yok' }}</p></a>@empty<div class="rounded-xl border border-dashed p-12 text-center text-sm text-slate-500 md:col-span-2">Arabuluculuk kaydı bulunamadı.</div>@endforelse</div>@if($mediations->hasPages())<div class="mt-6">{{ $mediations->links() }}</div>@endif</x-layouts.app>
