<x-layouts.app title="Hukuki Takvim"><div class="flex flex-wrap items-end justify-between gap-4"><div><p class="text-xs font-semibold uppercase tracking-[0.18em] text-indigo-600">Birleşik Görünüm</p><h1 class="mt-1 text-3xl font-bold text-slate-950">Hukuki Takvim</h1><p class="mt-1 text-sm text-slate-500">Duruşma, süre ve görevler tek kronolojik akışta.</p></div><div class="flex gap-2"><a class="rounded-lg border border-slate-300 bg-white px-3 py-2 text-sm" href="{{ route('hearings.create') }}">Duruşma</a><a class="rounded-lg border border-slate-300 bg-white px-3 py-2 text-sm" href="{{ route('deadlines.create') }}">Süre</a><a class="rounded-lg bg-indigo-700 px-3 py-2 text-sm font-semibold text-white" href="{{ route('legal-tasks.create') }}">Görev</a></div></div><form class="mt-6 flex flex-wrap gap-3 rounded-xl border border-slate-200 bg-white p-4 shadow-sm" method="GET"><input type="date" name="from" value="{{ request('from', $from->format('Y-m-d')) }}" class="rounded-lg border-slate-300"><input type="date" name="to" value="{{ request('to', $to->format('Y-m-d')) }}" class="rounded-lg border-slate-300"><button class="rounded-lg bg-slate-900 px-4 py-2 text-sm font-semibold text-white">Göster</button></form><div class="mt-6 space-y-6">@forelse($entriesByDate as $date => $entries)<section><div class="sticky top-20 z-10 inline-flex rounded-full bg-slate-900 px-4 py-1.5 text-sm font-semibold text-white">{{ Carbon\Carbon::parse($date)->translatedFormat('d F Y, l') }}</div><div class="mt-3 grid gap-3">@foreach($entries as $entry)<a class="flex items-center gap-4 rounded-xl border border-slate-200 bg-white p-4 shadow-sm hover:border-indigo-200" href="{{ $entry['url'] }}"><div class="flex h-12 w-12 shrink-0 items-center justify-center rounded-xl {{ $entry['type'] === 'hearing' ? 'bg-indigo-100 text-indigo-700' : ($entry['type'] === 'deadline' ? 'bg-amber-100 text-amber-700' : 'bg-sky-100 text-sky-700') }}"><span class="text-xs font-bold">{{ $entry['at']->format('H:i') }}</span></div><div class="min-w-0 flex-1"><div class="flex flex-wrap items-center gap-2"><span class="text-xs font-semibold uppercase tracking-wide text-slate-500">{{ $entry['type_label'] }}</span>@if($entry['case_no'])<span class="font-mono text-xs text-indigo-600">{{ $entry['case_no'] }}</span>@endif</div><p class="mt-1 break-words font-semibold text-slate-900">{{ $entry['title'] }}</p></div><span class="text-xs text-slate-500">{{ $entry['status'] }}</span></a>@endforeach</div></section>@empty<div class="rounded-xl border border-dashed border-slate-300 p-14 text-center text-sm text-slate-500">Seçilen aralıkta takvim kaydı bulunmuyor.</div>@endforelse</div></x-layouts.app>
