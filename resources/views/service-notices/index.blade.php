<x-layouts.app title="Tebligatlar"><div class="flex flex-wrap items-end justify-between gap-4"><div><p class="text-xs font-semibold uppercase tracking-[0.18em] text-rose-600">Hukuki Bildirim</p><h1 class="mt-1 text-3xl font-bold text-slate-950">Tebligatlar</h1></div><a class="rounded-lg bg-rose-700 px-4 py-2.5 text-sm font-semibold text-white" href="{{ route('service-notices.create') }}">Yeni Tebligat</a></div><form class="mt-6 flex flex-wrap gap-3 rounded-xl border border-slate-200 bg-white p-4" method="GET"><select name="case_file" class="rounded-lg border-slate-300"><option value="">Tüm dosyalar</option>@foreach($caseFiles as $caseFile)<option value="{{ $caseFile->id }}" @selected((string)request('case_file')===(string)$caseFile->id)>{{ $caseFile->case_no }}</option>@endforeach</select><select name="type" class="rounded-lg border-slate-300"><option value="">Tüm türler</option>@foreach(App\ServiceNoticeType::cases() as $type)<option value="{{ $type->value }}" @selected(request('type')===$type->value)>{{ $type->label() }}</option>@endforeach</select><button class="rounded-lg bg-slate-900 px-4 py-2 text-sm font-semibold text-white">Filtrele</button></form><div class="mt-6 space-y-3">@forelse($notices as $notice)<a class="flex flex-wrap items-center justify-between gap-4 rounded-xl border border-slate-200 bg-white p-5 shadow-sm" href="{{ route('service-notices.edit',$notice) }}"><div><span class="font-mono text-xs font-semibold text-indigo-600">{{ $notice->caseFile->case_no }}</span><h2 class="mt-1 font-semibold text-slate-900">{{ $notice->type->label() }}</h2><p class="mt-1 text-sm text-slate-500">{{ $notice->sender ?: '-' }} → {{ $notice->recipient ?: '-' }}</p></div><div class="text-right"><p class="font-semibold text-rose-700">{{ $notice->service_date->format('d.m.Y') }}</p><p class="text-xs text-slate-500">Tebliğ tarihi</p></div></a>@empty<div class="rounded-xl border border-dashed p-12 text-center text-sm text-slate-500">Tebligat bulunamadı.</div>@endforelse</div>@if($notices->hasPages())<div class="mt-6">{{ $notices->links() }}</div>@endif</x-layouts.app>
