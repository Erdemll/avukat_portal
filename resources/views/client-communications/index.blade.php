<x-layouts.app title="Müşteri İletişimleri"><div class="flex items-end justify-between gap-4"><div><p class="text-xs font-semibold uppercase tracking-[0.18em] text-sky-600">İletişim Geçmişi</p><h1 class="mt-1 text-3xl font-bold">Müşteri İletişimleri</h1></div><a class="rounded-lg bg-sky-700 px-4 py-2.5 text-sm font-semibold text-white" href="{{ route('client-communications.create') }}">Yeni İletişim</a></div><div class="mt-6 space-y-3">@forelse($communications as $communication)<a class="block rounded-xl border border-slate-200 bg-white p-5 shadow-sm" href="{{ route('client-communications.show',$communication) }}"><div class="flex flex-wrap items-start justify-between gap-3"><div><span class="text-xs font-semibold text-sky-600">{{ $communication->type->label() }} · {{ $communication->client->party->display_name }}</span><h2 class="mt-1 font-semibold">{{ $communication->subject }}</h2><p class="mt-2 line-clamp-2 text-sm text-slate-600">{{ $communication->description }}</p></div><div class="text-right text-sm"><p class="font-medium">{{ $communication->communication_at->format('d.m.Y H:i') }}</p><p class="text-xs text-slate-500">{{ $communication->user->name }}@if($communication->caseFile) · {{ $communication->caseFile->case_no }}@endif</p></div></div></a>@empty<div class="rounded-xl border border-dashed p-12 text-center text-sm text-slate-500">İletişim kaydı bulunamadı.</div>@endforelse</div>@if($communications->hasPages())<div class="mt-6">{{ $communications->links() }}</div>@endif</x-layouts.app>
