<x-layouts.app :title="$clientCommunication->subject">
    <div class="flex flex-wrap items-start justify-between gap-4"><div><a class="text-sm text-slate-500" href="{{ route('client-communications.index') }}">&larr; İletişimler</a><p class="mt-4 text-xs font-semibold uppercase tracking-wider text-sky-600">{{ $clientCommunication->type->label() }}</p><h1 class="mt-1 text-2xl font-bold">{{ $clientCommunication->subject }}</h1></div>@if($canUpdate)<a class="rounded-lg border border-slate-300 px-4 py-2 text-sm font-semibold" href="{{ route('client-communications.edit',$clientCommunication) }}">Düzenle</a>@endif</div><section class="mt-6 max-w-3xl rounded-xl border border-slate-200 bg-white p-6 shadow-sm"><dl class="grid gap-4 sm:grid-cols-3"><div><dt class="text-xs text-slate-500">Müvekkil</dt><dd class="mt-1 font-medium">{{ $clientCommunication->client->party->display_name }}</dd></div><div><dt class="text-xs text-slate-500">Kaydeden</dt><dd class="mt-1 font-medium">{{ $clientCommunication->user->name }}</dd></div><div><dt class="text-xs text-slate-500">Tarih</dt><dd class="mt-1 font-medium">{{ $clientCommunication->communication_at->format('d.m.Y H:i') }}</dd></div>@if($clientCommunication->caseFile)<div><dt class="text-xs text-slate-500">Hukuki Dosya</dt><dd class="mt-1 font-medium"><a class="text-indigo-600" href="{{ route('case-files.show',$clientCommunication->caseFile) }}">{{ $clientCommunication->caseFile->case_no }}</a></dd></div>@endif</dl><div class="mt-6 whitespace-pre-line border-t border-slate-100 pt-5 text-sm leading-6 text-slate-700">{{ $clientCommunication->description }}</div></section>
</x-layouts.app>
