<x-layouts.app title="Sürelerim"><div class="flex flex-wrap items-end justify-between gap-4"><div><p class="text-xs font-semibold uppercase tracking-[0.18em] text-amber-600">Süre Yönetimi</p><h1 class="mt-1 text-3xl font-bold text-slate-950">Sürelerim</h1></div><a class="rounded-lg bg-amber-600 px-4 py-2.5 text-sm font-semibold text-white" href="{{ route('deadlines.create') }}">Yeni Süre</a></div><div class="mt-5 flex gap-2"><a class="rounded-full bg-white px-4 py-2 text-sm ring-1 ring-slate-200" href="{{ route('deadlines.index') }}">Tümü</a><a class="rounded-full bg-white px-4 py-2 text-sm ring-1 ring-slate-200" href="{{ route('deadlines.index', ['period' => 'week']) }}">7 Gün İçinde</a><a class="rounded-full bg-white px-4 py-2 text-sm ring-1 ring-slate-200" href="{{ route('deadlines.index', ['period' => 'overdue']) }}">Süresi Geçen</a></div><div class="mt-6 space-y-3">@forelse($deadlines as $deadline)<a class="flex flex-wrap items-center justify-between gap-4 rounded-xl border border-slate-200 bg-white p-5 shadow-sm" href="{{ route('deadlines.edit', $deadline) }}"><div><span class="font-mono text-xs font-semibold text-indigo-600">{{ $deadline->caseFile->case_no }}</span><h2 class="mt-1 font-semibold text-slate-900">{{ $deadline->title }}</h2><p class="mt-1 text-sm text-slate-500">{{ $deadline->assignedLawyer?->name ?? 'Sorumlu yok' }} · {{ $deadline->status->label() }}</p></div><div class="text-right"><p class="font-semibold {{ $deadline->status === App\DeadlineStatus::Open && $deadline->due_at->isPast() ? 'text-red-700' : 'text-amber-700' }}">{{ $deadline->due_at->format('d.m.Y H:i') }}</p><p class="mt-1 text-xs text-slate-500">{{ $deadline->status === App\DeadlineStatus::Open ? $deadline->due_at->diffForHumans() : '' }}</p></div></a>@empty<div class="rounded-xl border border-dashed border-slate-300 p-14 text-center text-sm text-slate-500">Süre kaydı bulunamadı.</div>@endforelse</div>@if($deadlines->hasPages())<div class="mt-6">{{ $deadlines->links() }}</div>@endif</x-layouts.app>
