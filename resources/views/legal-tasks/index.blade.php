<x-layouts.app title="Görevlerim"><div class="flex flex-wrap items-end justify-between gap-4"><div><p class="text-xs font-semibold uppercase tracking-[0.18em] text-sky-600">İş Yönetimi</p><h1 class="mt-1 text-3xl font-bold text-slate-950">Görevlerim</h1></div><a class="rounded-lg bg-sky-700 px-4 py-2.5 text-sm font-semibold text-white" href="{{ route('legal-tasks.create') }}">Yeni Görev</a></div><div class="mt-5 flex gap-2"><a class="rounded-full bg-white px-4 py-2 text-sm ring-1 ring-slate-200" href="{{ route('legal-tasks.index', ['scope' => 'mine']) }}">Bana Atananlar</a><a class="rounded-full bg-white px-4 py-2 text-sm ring-1 ring-slate-200" href="{{ route('legal-tasks.index', ['status' => 'completed']) }}">Tamamlananlar</a></div><div class="mt-6 grid gap-3 lg:grid-cols-2">@forelse($tasks as $task)<a class="rounded-xl border border-slate-200 bg-white p-5 shadow-sm" href="{{ route('legal-tasks.edit', $task) }}"><div class="flex items-start justify-between gap-3"><div><span class="text-xs font-semibold text-indigo-600">{{ $task->caseFile?->case_no ?? 'Kişisel Görev' }}</span><h2 class="mt-1 font-semibold text-slate-900">{{ $task->title }}</h2></div><span class="rounded-full bg-slate-100 px-2 py-1 text-xs">{{ $task->status->label() }}</span></div><p class="mt-3 text-sm text-slate-500">{{ $task->assignee->name }} · {{ $task->due_at?->format('d.m.Y H:i') ?? 'Tarihsiz' }}</p></a>@empty<div class="rounded-xl border border-dashed border-slate-300 p-14 text-center text-sm text-slate-500 lg:col-span-2">Görev bulunamadı.</div>@endforelse</div>@if($tasks->hasPages())<div class="mt-6">{{ $tasks->links() }}</div>@endif</x-layouts.app>
