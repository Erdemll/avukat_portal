<x-layouts.app title="Duruşmalarım">
    <div class="flex flex-wrap items-end justify-between gap-4"><div><p class="text-xs font-semibold uppercase tracking-[0.18em] text-indigo-600">Hukuki Takvim</p><h1 class="mt-1 text-3xl font-bold text-slate-950">Duruşmalarım</h1></div><a class="rounded-lg bg-indigo-700 px-4 py-2.5 text-sm font-semibold text-white" href="{{ route('hearings.create') }}">Yeni Duruşma</a></div>
    <div class="mt-5 flex gap-2 overflow-x-auto"><a class="rounded-full bg-white px-4 py-2 text-sm ring-1 ring-slate-200" href="{{ route('hearings.index') }}">Tümü</a><a class="rounded-full bg-white px-4 py-2 text-sm ring-1 ring-slate-200" href="{{ route('hearings.index', ['period' => 'today']) }}">Bugün</a><a class="rounded-full bg-white px-4 py-2 text-sm ring-1 ring-slate-200" href="{{ route('hearings.index', ['period' => 'week']) }}">Bu Hafta</a></div>
    <div class="mt-6 space-y-3">@forelse($hearings as $hearing)<a class="flex flex-wrap items-center justify-between gap-4 rounded-xl border border-slate-200 bg-white p-5 shadow-sm hover:border-indigo-200" href="{{ route('hearings.edit', $hearing) }}"><div><span class="font-mono text-xs font-semibold text-indigo-600">{{ $hearing->caseFile->case_no }}</span><h2 class="mt-1 font-semibold text-slate-900">{{ $hearing->title }}</h2><p class="mt-1 text-sm text-slate-500">{{ $hearing->court ?: 'Mahkeme belirtilmedi' }} · {{ $hearing->lawyer?->name ?? 'Avukat belirtilmedi' }}</p></div><div class="text-right"><p class="font-semibold text-slate-900">{{ $hearing->hearing_at->format('d.m.Y H:i') }}</p><p class="mt-1 text-xs text-slate-500">{{ $hearing->status->label() }}</p></div></a>@empty<div class="rounded-xl border border-dashed border-slate-300 p-14 text-center text-sm text-slate-500">Duruşma bulunamadı.</div>@endforelse</div>
    @if($hearings->hasPages())<div class="mt-6">{{ $hearings->links() }}</div>@endif
</x-layouts.app>
